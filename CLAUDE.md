# قاعده‌های این مخزن

یک ورکر کلادفلر، دو سایت: `sensacare.ir` (فروشگاه سِنسا) و `sltech.ir`
(کارتابل‌ها + پنل مدیریت + سایت فروش). داده در یک D1.

## بعدِ هر تغییر، کُد را تمیز کن

این همیشگی است، نه وقتی خواسته شود:

- تکرار را بردار. اگر همان منطق دو جا افتاد، یک تابع شود — مخصوصاً بین
  `public/_t/it.tpl` و `public/_t/fin.tpl` که خویشاوندند و راحت از هم
  جدا می‌افتند.
- کدِ مرده را پاک کن: تابعی که کسی صدایش نمی‌زند، شاخه‌ای که هیچ‌وقت
  گرفته نمی‌شود، متغیری که خوانده نمی‌شود.
- کاری که در یک رفت‌وبرگشت می‌شود، در حلقه نکن — به‌خصوص پرسش از D1.
- رنگ و اندازه از توکن‌های برند بیاید، نه هگزِ سفت‌شده.
- ولی خوانایی بالاتر از کوتاهی است، و توضیحِ «چرا» بماند. تمیزکاری
  یعنی کدِ کمتر با همان رفتار، نه کدِ هوشمندتر.

تغییرِ رفتار را با تمیزکاری قاطی نکن؛ اگر چیزی باید عوض شود، بگو.

## شاخه و استقرار

- روی `claude/dazzling-dirac-a5e75m` کار کن.
- انتشار با پوش همان کامیت به
  `claude/health-products-cloudflare-worker-5z2moc` انجام می‌شود، که
  اکشنِ `.github/workflows/deploy.yml` را راه می‌اندازد. آن اکشن با
  رازِ `CLOUDFLARE_API_TOKEN` روی کلادفلر منتشر می‌کند — پس Roll کردنِ
  آن توکن استقرار را می‌خواباند تا وقتی رازِ گیت‌هاب به‌روز شود.
- PR فقط وقتی خواسته شود.

## چیزهایی که نباید بشکنند

- **هیچ پشتیبانی به کاربرها داده یا فرستاده نمی‌شود** — نه در تلگرام،
  نه جای دیگر. پشتیبان فقط برای مدیر است.
- کلیدِ رمزنگاریِ «دیتای شخصی» هیچ‌وقت به سرور نمی‌رسد. کدهای بازیابی
  یک بار از سرور رد می‌شوند و ذخیره نمی‌شوند.
- هیچ توکنی — کلادفلر، تلگرام، بله — داخل مخزن نوشته نمی‌شود.
- `migrations.sql` هر بار موقع استقرار اجرا می‌شود، پس فقط
  `CREATE ... IF NOT EXISTS`. `ALTER TABLE` بارِ دوم کلِ استقرار را
  می‌خواباند؛ ستونِ تازه را در همان ماژول داخلِ `try` اضافه کن (مثل
  `ensureShared` در `src/shared.js`).
- قاعده‌های دسترسی روی سرور اعمال می‌شوند. خانهٔ غیرفعالِ مرورگر ادب
  است، قفل نیست.

## آزمون

آزمون‌ها با Playwright از
`/opt/node22/lib/node_modules/playwright` اجرا می‌شوند و در پوشهٔ
scratchpad همین نشست‌اند، نه در مخزن.

سرور محلی:

```
npx wrangler dev --local --port 8787
npx wrangler d1 execute sensa-db --local --command "…"
```

نکته: `page.on('dialog', d => d.accept())` لازم است، و `state` در
قالب‌ها با `let` تعریف شده پس روی `window` نمی‌نشیند.
