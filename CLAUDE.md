# سِنسا و SLTech — قاعده‌های کار روی این مخزن

یک ورکر کلادفلر، دو سایت: `sensacare.ir` (فروشگاه سِنسا) و `sltech.ir`
(کارتابل‌ها + پنل مدیریت + سایت فروش). داده در یک D1.

> بیش از یک نشست هم‌زمان روی این مخزن کار می‌کند. پیش از push حتماً
> `git fetch` و اگر جلو افتاده بود **merge** کن — هیچ‌وقت force push.

## بعد از هر تغییر، یک دور بهینه‌سازی

هر بار که کاری انجام شد، پیش از commit این چهار مورد را روی همان چیزی
که دست خورده بررسی کن — نه روی کل مخزن:

1. **کد مرده** — تابع، متغیر، قاعدهٔ CSS یا شاخه‌ای که دیگر هیچ‌کس
   صدایش نمی‌زند. پیدا کردی، بردار.
2. **تکرار** — اگر همان منطق دو جا نوشته شده، یکی‌اش کن. مخصوصاً بین
   `src/index.js` و `public/admin.js` (هر دو با همان دادهٔ فروشگاه کار
   می‌کنند) و بین `public/_t/it.tpl` و `public/_t/fin.tpl` (دو قالبِ
   خویشاوند که راحت از هم جدا می‌افتند).

   برای همین دو قالب راهش آماده است: هر تکه‌ای که در هر دو یکی است در
   `public/_t/p/<نام>.tpl` می‌نشیند و در قالب فقط `{{PART:<نام>}}`
   می‌ماند. `renderPanelPage` آن را سمتِ سرور سرِ جایش می‌گذارد، پس
   خروجی مو‌به‌مو همان HTMLِ قبلی است و پشتیبانِ آفلاین هم دست‌نخورده
   می‌ماند. جای تکه‌ها پیش از جایگزینیِ `{{TITLE}}` و امثالش است، پس
   placeholderهای داخلِ تکه هم پُر می‌شوند.

   دو نکته: تکه‌ای که پیدا نشود صفحه را نصفه‌نیمه نمی‌کند (کلِ رندر
   null می‌شود)؛ و قالبِ سرِهم‌شده در `TPL_CACHE` می‌ماند، چون صفحهٔ
   کارتابل `no-store` است و وگرنه هر بار باز شدنش همهٔ تکه‌ها را
   دوباره می‌خواند.

   **چیزی که در هر دو قالب یکی است را به `public/shared.js` نبر:**
   آن فایل فقط بعد از ورود بار می‌شود (`setupShared` روی `signedIn`
   شرط دارد)، پس در پشتیبانِ آفلاین اصلاً بار نمی‌شود. تکه‌ها این
   مشکل را ندارند چون داخلِ خودِ HTML می‌نشینند.
3. **حجم** — فروشگاه تک‌صفحه‌ای است و `public/index.html` روی اینترنت
   موبایل ایران دانلود می‌شود. هر کیلوبایت اضافه هزینه دارد.
4. **کار اضافه** — پرس‌وجوی تکراری به D1، درخواست شبکه‌ای که لازم نبود،
   حلقه‌ای که می‌شد یک کوئری باشد. D1 روی شبکه است: یک کوئری داخل حلقه
   گران است.

قاعده‌ای که بر همهٔ اینها مقدم است: **اگر تغییری رفتار را عوض می‌کند،
بهینه‌سازی نیست.** بهینه‌سازی یعنی همان رفتار با کد کمتر یا کار کمتر.
اگر مطمئن نیستی، دست نزن و به‌جایش پیشنهادش را بگو.

و: **اندازه بگیر، حدس نزن.** قبل و بعدِ هر بهینه‌سازی عدد داشته باش
(حجم فایل، تعداد کوئری، زمان پاسخ)، وگرنه معلوم نیست بهتر شده یا نه.

ولی خوانایی بالاتر از کوتاهی است، و توضیحِ «چرا» می‌ماند.

## خط قرمزهای فروشگاه

- **هیچ شمارهٔ تلفنی هیچ‌جای سایت** — سیاست قطعی صاحب سایت
- هیچ نظر یا امتیاز ساختگی
- پیام‌های ربات هیچ‌وقت نام کالا را نمی‌گویند (قولِ محرمانگی)
- درگاه پرداخت و ای‌نماد عمداً نیستند — اضافه نکن
- همهٔ عددهای دیده‌شدنی فارسی

## خط قرمزهای کارتابل‌ها

- **پشتیبانِ کارتابل هیچ فایلی از فروشگاه برنمی‌دارد** و به پشتیبانِ
  فروشگاه هم کاری ندارد. دو چیزِ جداست که فقط روی یک ورکر نشسته‌اند.

- **هیچ پشتیبانی به کاربرها داده یا فرستاده نمی‌شود** — نه در تلگرام،
  نه جای دیگر. پشتیبان فقط برای مدیر است.
- کلیدِ رمزنگاریِ «دیتای شخصی» هیچ‌وقت به سرور نمی‌رسد. کدهای بازیابی
  یک بار از سرور رد می‌شوند و ذخیره نمی‌شوند.
- قاعده‌های دسترسی روی سرور اعمال می‌شوند. خانهٔ غیرفعالِ مرورگر ادب
  است، قفل نیست.

## همیشگی

- هیچ توکنی — کلادفلر، تلگرام، بله — داخل مخزن نوشته نمی‌شود.
- `migrations.sql` هر بار موقع استقرار اجرا می‌شود: هرچه آنجا می‌نویسی
  باید اجرای دوباره‌اش بی‌خطر باشد، و **هیچ‌وقت `ALTER TABLE`** — بارِ
  دوم کلِ استقرار را می‌خواباند. ستون تازه یا در جدول خودش می‌نشیند
  (مثل `order_discounts`) یا همان ماژول داخلِ `try` اضافه‌اش می‌کند
  (مثل `ensureShared` در `src/shared.js`).

## شاخه و استقرار

- روی `claude/dazzling-dirac-a5e75m` کار کن.
- انتشار با پوشِ همان کامیت به
  `claude/health-products-cloudflare-worker-5z2moc` انجام می‌شود، که
  اکشنِ `.github/workflows/deploy.yml` را راه می‌اندازد. آن اکشن با
  رازِ `CLOUDFLARE_API_TOKEN` روی کلادفلر منتشر می‌کند — پس Roll کردنِ
  آن توکن استقرار را می‌خواباند تا وقتی رازِ گیت‌هاب به‌روز شود.
- PR فقط وقتی خواسته شود.

## پیش از push

- `npx esbuild --bundle src/index.js --outfile=/dev/null --format=esm`
- `node --check public/admin.js` و `node --check public/shared.js`
- سرور محلی بالا بیاور و صفحه‌ها را در مرورگر واقعی باز کن؛ خطای
  کنسول نباید باشد
- عرض‌های ۳۲۰ و ۳۹۰ و ۱۲۸۰ — بیرون‌زدگی افقی نباشد
- `migrations.sql` را دو بار پشت‌سرهم اجرا کن؛ بار دوم باید بی‌اثر باشد
- بعد از استقرار با curl مطمئن شو تغییر واقعاً روی سایت نشسته — به سبز
  بودن گیت‌هاب اکتفا نکن

## آزمون

آزمون‌ها با Playwright از `/opt/node22/lib/node_modules/playwright`
اجرا می‌شوند و در پوشهٔ scratchpad همان نشست‌اند، نه در مخزن.

```
npx wrangler dev --local --port 8787
npx wrangler d1 execute sensa-db --local --command "…"
```

نکته: `page.on('dialog', d => d.accept())` لازم است، و `state` در
قالب‌های کارتابل با `let` تعریف شده پس روی `window` نمی‌نشیند.

**یک کارتابل، یک جا:** هر ورود یک `sid` تازه می‌سازد و در
`settings` زیر کلیدِ `sid:<slug>` ثبتش می‌کند؛ `readSession` هر
توکنی را که `sid`ش با آن نخواند با `TAKEN` برمی‌گرداند — که یک شیء
است، نه `null`. پس **هر صدازنندهٔ تازهٔ `readSession` باید `TAKEN` را
صریح رد کند**، وگرنه `if (!session)` از کنارش رد می‌شود و در باز
می‌ماند. پنلِ ادمین با `solo:false` بیرونِ این قفل است. سنجه‌اش
`solo.mjs` است (دو context با دو کوکی‌جار = دو دستگاه).
