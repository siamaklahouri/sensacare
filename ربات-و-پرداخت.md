# پرداخت و ربات‌ها

## چطور کار می‌کند

۱. مشتری سفارش می‌دهد و یک **شمارهٔ فاکتور یکتا** می‌گیرد، مثل `SNS-10001`
۲. همان لحظه فاکتور کامل در **تلگرام و بله** برای شما می‌آید، با دو دکمه
۳. مشتری شمارهٔ فاکتور را در پیام‌رسان برایتان می‌فرستد
۴. شما شماره کارت می‌دهید
۵. مشتری واریز می‌کند و فیش را همان‌جا می‌فرستد
۶. شما دکمهٔ **✅ پرداخت شد** را می‌زنید
۷. سفارش خودکار تأیید می‌شود، وضعیتش در سایت عوض می‌شود، پیام تأیید برای شما در هر دو پیام‌رسان می‌آید، و اگر مشتری با ربات حرف زده باشد به او هم خبر می‌رود

---

## چرا تأیید کاملاً خودکار نیست؟

هیچ رباتی نمی‌تواند بفهمد پول به کارت شما رسیده. برای این کار باید به حساب بانکی وصل بود که فقط با درگاه رسمی و نماد اعتماد ممکن است. پس یک دکمه زدن، کوتاه‌ترین راه ممکن است. باقی کارها خودکار انجام می‌شود.

---

## راه‌اندازی

### ۱. ربات تلگرام بسازید

۱. در تلگرام به `@BotFather` پیام بدهید
۲. بنویسید `/newbot`
۳. یک اسم بدهید، مثلاً `سنسا`
۴. یک آی‌دی بدهید که به `bot` ختم شود، مثلاً `sensacare_bot`
۵. توکنی می‌دهد شبیه `7712345678:AAF...` — کپی کنید

### ۲. ربات بله بسازید

در اپ بله هم `@BotFather` هست. دقیقاً همین مراحل.

### ۳. وصل کردن

در پوشهٔ پروژه:

```
npm run bots
```

توکن‌ها و آدرس سایت را می‌پرسد، بقیه‌اش خودکار است.

### ۴. ثبت خودتان به‌عنوان مدیر

در هر دو ربات این پیام را بفرستید:

```
/admin رمزِ‌پنلِ‌مدیریت
```

همان رمزی که موقع نصب گذاشتید. ربات جواب می‌دهد «این گفتگو به‌عنوان مدیر ثبت شد».

از این لحظه فاکتورها می‌آید.

> می‌توانید ربات را به یک **گروه** اضافه کنید و همان‌جا `/admin رمز` را بفرستید تا فاکتورها در گروه بیاید و چند نفر ببینند.

### ۵. تست

در پنل مدیریت ← تنظیمات ← دکمهٔ **ارسال پیام آزمایشی**.

---

## کارهایی که ربات انجام می‌دهد

| مشتری بفرستد | ربات چه می‌کند |
|---|---|
| `/start` | معرفی و راهنما |
| شمارهٔ فاکتور | وضعیت سفارش و پرداخت را می‌گوید |
| عکس فیش | به شما اطلاع می‌دهد |

| شما بفرستید | نتیجه |
|---|---|
| `/admin رمز` | این گفتگو مدیر می‌شود |
| دکمهٔ ✅ | سفارش تأیید و مشتری مطلع می‌شود |
| دکمهٔ ❌ | سفارش لغو می‌شود |

---

## نکته‌ها

**آی‌دی تلگرام روی سایت** جداست و در تنظیمات پنل تغییر می‌کند. آن لینکی است که مشتری با آن به شما پیام می‌دهد.

**اگر ربات پیام نفرستاد:** مطمئن شوید `/admin رمز` را فرستاده‌اید. بدون آن، ربات نمی‌داند فاکتور را کجا بفرستد.

**بله همیشه پایدار نیست.** اگر پیام‌های بله نرسید ولی تلگرام کار کرد، مشکل از سمت بله است نه سایت.

**شماره‌های فاکتور ترتیبی و بدون تکرارند** — از `SNS-10001` شروع می‌شوند و هر سفارش یکی جلو می‌رود.
