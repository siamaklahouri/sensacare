# راه‌اندازی خودکار سِنسا

## فقط دو کار لازم است

**۱. Node.js را نصب کنید** (اگر ندارید)

از `nodejs.org` نسخهٔ **LTS** را بگیرید، نصب کنید، کامپیوتر را **ری‌استارت** کنید.

**۲. فایل راه‌انداز را اجرا کنید**

- **ویندوز:** روی `راه-اندازی-ویندوز.bat` دوبار کلیک کنید
- **مک:** روی `راه-اندازی-مک.command` دوبار کلیک کنید

> **مک:** اگر گفت «نمی‌توان باز کرد چون از توسعه‌دهندهٔ شناسایی‌شده نیست»، روی فایل راست‌کلیک کنید و **Open** را بزنید، بعد **Open** را تأیید کنید.

همین. بقیه‌اش خودکار است.

---

## اسکریپت چه می‌کند

۱. Node را چک می‌کند
۲. ابزارها را نصب می‌کند
۳. شما را وارد کلادفلر می‌کند
۴. دیتابیس می‌سازد و شناسه‌اش را خودش در فایل تنظیمات می‌گذارد
۵. جدول‌ها را می‌سازد
۶. کلید امنیتی را خودش تولید می‌کند و رمز مدیر را از شما می‌پرسد
۷. سرویس پیامک را می‌پرسد (می‌توانید رد کنید)
۸. سایت را بالا می‌آورد
۹. محصولات و مقالات را می‌سازد
۱۰. آدرس سایت را به شما می‌دهد

---

## دو جا خودتان باید کاری کنید

**ورود به کلادفلر.** اسکریپت مرورگر را باز می‌کند، شما **Allow** را بزنید. اگر باز نشد، فیلترشکن را روشن کنید.

**وصل کردن دامنه.** این کار از خط فرمان ممکن نیست. بعد از اینکه کلادفلر دامنه را فعال کرد:

۱. `dash.cloudflare.com` → **Workers & Pages** → روی `sensa`
۲. **Settings** → **Domains & Routes** → **Add** → **Custom domain**
۳. دامنه‌تان را بنویسید

---

## دستورهای بعدی

بعد از راه‌اندازی، در همین پوشه:

| دستور | کار |
|---|---|
| `npm run update` | به‌روزرسانی سایت بعد از تغییر فایل |
| `npm run sms` | تنظیم سرویس پیامک |
| `npm run backup` | گرفتن پشتیبان کامل از دیتابیس |
| `npm run setup` | اجرای دوبارهٔ راه‌انداز |

اجرای دوبارهٔ `setup` خطرناک نیست — هر چیزی که از قبل ساخته شده را دست نمی‌زند.

---

## اگر خطا داد

**`node: command not found`** → Node نصب نشده یا کامپیوتر ری‌استارت نشده.

**مرورگر برای ورود باز نمی‌شود** → فیلترشکن را روشن کنید.

**`npm install` گیر می‌کند** → اینترنت به npm وصل نمی‌شود، DNS را عوض کنید یا فیلترشکن روشن کنید.

**سایت باز می‌شود ولی خالی است** → یک بار رفرش کنید، بار اول چند ثانیه طول می‌کشد.

**آدرس workers.dev باز نمی‌شود** → از ایران فیلتر است. با فیلترشکن باز کنید. بعد از وصل کردن دامنه حل می‌شود.

هر خطای دیگری دیدید، متنش را کپی کنید و بفرستید.

---

## راه سوم: بدون هیچ نصبی، با «توکن»

اگر نمی‌خواهید چیزی روی کامپیوترتان نصب کنید، یا می‌خواهید سایت
خودش بعد از هر تغییر به‌روز شود:

۱. فایل **`راهنمای-توکن-کلادفلر.md`** را باز کنید
۲. طبق آن یک توکن بسازید (۱۰ قدم ساده، تصویری توضیح داده شده)
۳. توکن و شناسهٔ حساب را در گیت‌هاب به عنوان Secret ذخیره کنید

از آن به بعد سایت خودکار روی کلادفلر بالا می‌رود.

اگر هم ترجیح می‌دهید خودتان دستی اجرا کنید:

```
export CLOUDFLARE_API_TOKEN="..."
export CLOUDFLARE_ACCOUNT_ID="..."
npm install
npm run deploy
```

`npm run deploy` هیچ سؤالی نمی‌پرسد و مرورگر باز نمی‌کند —
دیتابیس را می‌سازد، جدول‌ها را می‌چیند، رمزها را می‌گذارد و سایت را بالا می‌آورد.
اگر جدول‌ها از قبل باشند، دست به داده‌هایتان نمی‌زند.
