# کارتابل مدیر مالی — `sensacare.ir/sina`

کارتابل مالی همان کاری را می‌کند که کارتابل IT روی `/siamak` انجام می‌دهد،
فقط بخش‌هایش مالی است: اسناد دریافتنی، بدهی‌ها، اسناد پرداختنی، منابع و
مصارف، حساب‌های بانکی، بودجه‌بندی و طرف‌حساب‌ها.

## چه چیزی مشترک است و چه چیزی جدا

هر دو کارتابل از یک کد استفاده می‌کنند (`src/kartabl.js`، بخش `PANELS`).
جدول `kartabl` هم مشترک است، با کلیدهای متفاوت:

| | مدیر IT | مدیر مالی |
| --- | --- | --- |
| آدرس | `/siamak` | `/sina` |
| کلید داده | `state` / `db` | `sina:state` / `sina:db` |
| رمز | `kartablPassHash` | `sinaPassHash` |
| کوکی نشست | `kartabl_s` | `sina_s` |
| پوشه در زیپ | `It/` | `Mali/` |

**رمز و نشست هر کدام جداست** — ورود به یکی به آن یکی دسترسی نمی‌دهد. توکن
داخل کوکی شناسهٔ کارتابل را دارد و سرور بررسی می‌کند که با همان کارتابل
بخواند؛ کوکی یکی روی آن یکی کار نمی‌کند.

**ربات پشتیبان مشترک است**: هر شب دو زیپ جدا برای همان یک گفتگوی تلگرام
می‌رود، یکی `It/` و یکی `Mali/`. اگر یکی نرود، آن یکی قربانی نمی‌شود.

## دیتای شخصی

همان بخش کارتابل IT است، ولی خالی شروع می‌شود. بار اول که بازش کنید خودتان
یک رمز جداگانه می‌گذارید و از همان لحظه محتوایش با AES-256-GCM (کلید از
رمز شما با PBKDF2) رمز می‌شود. سرور فقط متن رمزشده را نگه می‌دارد و کلیدش
را ندارد — اگر روزی سرور هم لو برود، این بخش باز نمی‌شود.

## دو چیزی که سر راه درست شد

**اسناد پرداختنی و دریافتنی ذخیره نمی‌شدند.** نسخهٔ اصلی کارتابل مالی در
حافظهٔ مرورگر `payableNotes` و `receivableNotes` را نمی‌نوشت (در
`persistCache` از قلم افتاده بودند)، پس با هر بار باز کردن صفحه از بین
می‌رفتند. حالا هر دو در نسخهٔ سرور هستند.

**پشتیبان و بازیابی نداشت.** تنها راه بیرون بردن داده «اتصال به پوشه» بود
که فقط در کروم کار می‌کند. حالا مثل کارتابل IT دکمهٔ ⬇ پشتیبان و
⬆ بازیابی دارد.

## فایل‌ها

| فایل | کارش |
| --- | --- |
| `public/sina/index.html` | خودِ کارتابل مالی |
| `src/kartabl.js` | `PANELS` — تنظیمات هر دو کارتابل |
| `src/kartabl-xlsx.js` | `buildSinaWorkbook` — یازده برگهٔ فایل اکسل |
| `migrations.sql` | رمز اولیهٔ کارتابل مالی |
