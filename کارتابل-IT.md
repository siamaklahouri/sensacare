# کارتابل مدیر IT — `sensacare.ir/siamak`

کارتابل ماهانهٔ مدیر IT حالا روی همین دامنه نشسته و با آدرس
**https://sensacare.ir/siamak** باز می‌شود. هیچ ربطی به فروشگاه ندارد،
فقط کنارش زندگی می‌کند.

## داده‌ها کجا می‌مانند؟

**روی سرور، در دیتابیس D1 همین سایت.** با هر مرورگر و هر دستگاهی که وارد
شوید همان داده را می‌بینید. حافظهٔ مرورگر فقط نسخهٔ آفلاین است: اگر
اینترنت قطع باشد کارتابل باز می‌شود و کار می‌کند، و به‌محض وصل شدن
تغییرها بالا می‌روند.

دو تکه ذخیره می‌شود، همان دو تکه‌ای که خودِ کارتابل دارد:

| کلید در جدول `kartabl` | چه چیزی |
| --- | --- |
| `state` | وظایف، برنامهٔ روزانه، ماه‌های بایگانی، چک‌لیست ریموت، بخش شخصیِ رمزشده |
| `db` | سرورها، شرکت‌ها، خطوط MVPN، لاگ بکاپ روزانه، فهرست ریموت |

هر کدام یک JSON در یک ردیف است، نه جدولِ ستون‌بندی‌شده — ساختار کارتابل
مدام عوض می‌شود و این‌طور هر تغییر یک مهاجرت تازه نمی‌خواهد.

**اگر دو دستگاه هم‌زمان کار کنند**، شمارهٔ نسخه (`rev`) جلوی این را می‌گیرد
که دومی بی‌صدا روی کار اولی بنویسد: نواری پایین صفحه می‌آید و خودتان
انتخاب می‌کنید کدام نسخه بماند.

## قفل ورود

رمز **روی سرور** بررسی می‌شود، نه در مرورگر. به شکل `PBKDF2-SHA256` با
۳۱۰٬۰۰۰ دور در جدول `settings` می‌نشیند (کلید `kartablPassHash`)؛ نه در
فایل صفحه هست و نه از روی چیزی که ذخیره شده درمی‌آید. نشست یک کوکی
`HttpOnly` است که با همان `JWT_SECRET` فروشگاه امضا می‌شود — حتی کد خودِ
صفحه هم نمی‌تواند بخواندش.

تا وقتی داده فقط در مرورگر بود، قفلِ سمتِ مرورگر کافی بود: کسی که ردش
می‌کرد هم چیزی جز کارتابل خالی نمی‌دید. حالا که داده روی سرور است آن قفل
دیگر کافی نیست، و هیچ مسیری بدون کوکی معتبر داده نمی‌دهد.

**عوض کردن رمز:** از داخل خودِ کارتابل، بخش **⚙️ تنظیمات**. رمز فعلی لازم
است، و با عوض شدنش همهٔ دستگاه‌های دیگر که وارد مانده‌اند بیرون می‌افتند
(شمارهٔ نسل رمز بالا می‌رود و نشست‌های قدیمی از کار می‌افتند).

**بخش «دیتای شخصی»** همچنان با رمز جداگانهٔ خودش و در همین مرورگر
رمزنگاری می‌شود؛ سرور فقط متن رمزشده را نگه می‌دارد و کلیدش را ندارد.
این عمدی است: اگر سرور هم روزی لو برود، آن بخش باز نمی‌شود.

## پشتیبان شبانه در تلگرام

هر شب (همان کرون ۲۲:۰۰ گرینویچ که پشتیبان فروشگاه با آن می‌رود) یک زیپ
کامل برای ربات کارتابل فرستاده می‌شود — با همان سه فایل و همان ساختار
پوشه‌ای که کارتابل روی سیستم می‌سازد:

```
It/کارتابل-IT-داده.json        ← وضعیت کامل، شامل بخش شخصیِ رمزشده
It/کارتابل-IT-دیتابیس.xlsx      ← هشت برگه: سرورها، لاگ بکاپ، شرکت‌ها،
                                  MVPN، ریموت، وظایف، برنامهٔ روزانه، بخش شخصی
It/کارتابل مدیر IT.html        ← خودِ صفحه
```

با همین زیپ، کارتابل بدون سرور و بدون اینترنت باز می‌شود: فایل HTML را باز
می‌کنید و **⬆ بازیابی** را با فایل JSON می‌زنید.

ربات کارتابل از ربات فروشگاه جداست. توکنش در جدول `settings` می‌نشیند
(`kartablBotToken`)، **نه در این مخزن** — مخزن عمومی است. تنظیمش از بخش
**⚙️ تنظیمات** خودِ کارتابل انجام می‌شود:

۱. توکن را می‌گذارید و «ثبت توکن» را می‌زنید (با `getMe` بررسی می‌شود)
۲. در تلگرام به ربات `/start` می‌دهید
۳. «اتصال به گفتگوی من» را می‌زنید تا شناسهٔ گفتگو پیدا شود

دکمه‌های «همین حالا یک پشتیبان بفرست» و «دانلود زیپ پشتیبان» هم همان‌جا
هستند.

## چرا کتابخانه‌ها کپی شده‌اند؟

نسخهٔ اصلی Chart.js و SheetJS را از CDN و فونت را از Google Fonts می‌گرفت.
هر دو از داخل ایران ناپایدارند و سیاست امنیتی سایت هم جلویشان را می‌گیرد —
`script-src` فقط `'self'` و cdnjs را قبول می‌کند، پس jsdelivr و unpkg و
fastly روی این دامنه هرگز بالا نمی‌آمدند و فقط خطای کنسول می‌ساختند. حالا:

- `public/siamak/v/chart.umd.min.js` و `public/siamak/v/xlsx.full.min.js`
  روی خودِ دامنه سرو می‌شوند و فقط cdnjs به‌عنوان پشتیبان مانده.
- فونت وزیرمتن از همان `/f/` فروشگاه می‌آید.
- مهلت بارگذاری فایل خودی ۲۵ ثانیه است، نه ۶ ثانیه: بار اولی که فایل هنوز
  روی لبهٔ کلادفلر کش نشده از شش ثانیه رد می‌شود و صفحه بی‌دلیل سراغ CDN
  می‌رفت.

## چرا `/siamak` در کد ورکر مسیری ندارد؟

فایل‌های `public` پیش از رسیدن به کد ورکر از لبهٔ کلادفلر سرو می‌شوند. یعنی
`/siamak` خودش با یک ۳۰۷ به `/siamak/` می‌رود و فایل را می‌گیرد — مرورگر
این را بی‌صدا دنبال می‌کند. (`/admin` کد می‌خواهد چون فایلی به آن اسم وجود
ندارد و باید `index.html` فروشگاه برگردد.)

## فایل‌ها

| فایل | کارش |
| --- | --- |
| `public/siamak/index.html` | خودِ کارتابل |
| `public/siamak/v/*.js` | Chart.js و SheetJS |
| `src/kartabl.js` | ورود، ذخیرهٔ داده، تنظیمات ربات، پشتیبان |
| `src/kartabl-xlsx.js` | ساختن فایل اکسل (هشت برگه) |
| `src/kartabl-zip.js` | زیپ‌ساز — برای پشتیبان و برای خودِ فایل اکسل |
| `src/index.js` | خط `Disallow: /siamak` و صدا زدن پشتیبان شبانه |
| `migrations.sql` | جدول `kartabl` و رمز اولیه |
| `public/_headers` | هدر `noindex` برای `/siamak/*` |
