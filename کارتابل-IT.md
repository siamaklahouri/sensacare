# کارتابل مدیر IT — `sensacare.ir/siamak`

کارتابل ماهانهٔ مدیر IT حالا روی همین دامنه نشسته و با آدرس
**https://sensacare.ir/siamak** باز می‌شود. هیچ ربطی به فروشگاه ندارد،
فقط کنارش زندگی می‌کند.

## داده‌ها کجا می‌مانند؟

**روی سرور هیچ داده‌ای ذخیره نمی‌شود.** کارتابل همان برنامهٔ تک‌فایلی قبلی
است و همه‌چیز را در حافظهٔ همان مرورگری نگه می‌دارد که با آن کار می‌کنید
(`localStorage`). یعنی:

- اگر با مرورگر دیگری یا گوشی وارد شوید، کارتابل **خالی** است.
- بازدیدکنندهٔ ناشناسی که آدرس را حدس بزند هم کارتابل خالی می‌بیند، نه
  دادهٔ شما را.
- پاک کردن حافظهٔ مرورگر یعنی پاک شدن کارتابل. پس هر از گاهی دکمهٔ
  **⬇ پشتیبان** را بزنید و فایل JSON را جایی نگه دارید.

برای بردن داده از یک مرورگر به مرورگر دیگر: در اولی **⬇ پشتیبان** و در
دومی **⬆ بازیابی** با همان فایل.

## قفل ورود

صفحه یک قفل ورود دارد. رمز در فایل ذخیره **نشده**؛ فقط نمک و حاصل
`PBKDF2-SHA256` با ۳۱۰٬۰۰۰ دور در `public/siamak/index.html` است
(`GATE_SALT` و `GATE_HASH`).

این قفل سمت مرورگر است، نه سمت سرور — کسی که با کد صفحه سر و کله بزند
می‌تواند ردش کند. چون دادهٔ کارتابل اصلاً روی سرور نیست، بدترین حالتش این
است که یک کارتابل خالی ببیند.

**عوض کردن رمز:** آدرس `/siamak/set-password.html` را باز کنید، رمز تازه را
بنویسید، دو خطی که می‌سازد را جای `GATE_SALT` و `GATE_HASH` در
`public/siamak/index.html` بگذارید و push کنید. بعدش یک‌بار داخل کارتابل
**🔓 خروج از کارتابل** را بزنید تا رمز قدیمی از مرورگر پاک شود.

## چرا کتابخانه‌ها کپی شده‌اند؟

نسخهٔ اصلی Chart.js و SheetJS را از CDN و فونت را از Google Fonts می‌گرفت.
هر دو از داخل ایران ناپایدارند و سیاست امنیتی سایت هم جلویشان را می‌گیرد —
`script-src` فقط `'self'` و cdnjs را قبول می‌کند، پس jsdelivr و unpkg و
fastly روی این دامنه هرگز بالا نمی‌آمدند و فقط خطای کنسول می‌ساختند. حالا:

- `public/siamak/v/chart.umd.min.js` و `public/siamak/v/xlsx.full.min.js`
  روی خودِ دامنه سرو می‌شوند و فقط cdnjs به‌عنوان پشتیبان مانده.
- فونت وزیرمتن از همان `/f/` فروشگاه می‌آید.
- مهلت بارگذاری فایل خودی ۲۵ ثانیه است، نه ۶ ثانیه: بار اولی که فایل هنوز
  روی لبهٔ کلادفلر کش نشده از شش ثانیه رد می‌شود و صفحه بی‌دلیل سراغ CDN
  می‌رفت.

## چرا `/siamak` در کد ورکر مسیری ندارد؟

فایل‌های `public` پیش از رسیدن به کد ورکر از لبهٔ کلادفلر سرو می‌شوند. یعنی
`/siamak` خودش با یک ۳۰۷ به `/siamak/` می‌رود و فایل را می‌گیرد — مرورگر
این را بی‌صدا دنبال می‌کند. (`/admin` کد می‌خواهد چون فایلی به آن اسم وجود
ندارد و باید `index.html` فروشگاه برگردد.)

## فایل‌ها

| فایل | کارش |
| --- | --- |
| `public/siamak/index.html` | خودِ کارتابل |
| `public/siamak/set-password.html` | ساختن رمز تازه |
| `public/siamak/v/*.js` | Chart.js و SheetJS |
| `src/index.js` | خط `Disallow: /siamak` در robots.txt |
| `public/_headers` | هدر `noindex` برای `/siamak/*` |
