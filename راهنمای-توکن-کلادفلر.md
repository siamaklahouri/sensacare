# ساختن توکن کلادفلر — قدم به قدم، خیلی ساده

این فایل فقط یک کار را یاد می‌دهد: چطور در سایت کلادفلر یک «کلید» بسازید
تا سایت سِنسا خودکار روی کلادفلر بالا برود.

> **توکن یعنی چه؟**
> توکن مثل یک کلید یدکی خانه است. به کسی می‌دهید که فقط بتواند
> کارهای مشخصی انجام دهد. هر وقت خواستید هم کلید را باطل می‌کنید.

---

## قدم ۰ — چیزهایی که لازم دارید

* یک حساب رایگان در `dash.cloudflare.com` (اگر ندارید، با ایمیل بسازید)
* فیلترشکن روشن (پنل کلادفلر از ایران باز نمی‌شود)

---

## قدم ۱ — رفتن به صفحهٔ توکن‌ها

۱. برو به: **https://dash.cloudflare.com**
۲. وارد حسابت شو (ایمیل و رمز)
۳. **بالا سمت راست**، روی عکس گرد پروفایل کلیک کن
۴. از منویی که باز می‌شود، **My Profile** را بزن
۵. در ستون سمت چپ، **API Tokens** را بزن

> راه میان‌بر: مستقیم برو به
> **https://dash.cloudflare.com/profile/api-tokens**

---

## قدم ۲ — زدن دکمهٔ ساخت

روی دکمهٔ آبی **Create Token** کلیک کن.

حالا یک لیست از «قالب‌های آماده» می‌بینی.

---

## قدم ۳ — انتخاب قالب آماده

در لیست، دنبال این خط بگرد:

**Edit Cloudflare Workers**

روبه‌رویش دکمهٔ **Use template** را بزن.

> این قالب بیشتر دسترسی‌های لازم را از قبل دارد.
> فقط یک چیز کم دارد که در قدم بعد اضافه می‌کنیم.

---

## قدم ۴ — اضافه کردن دسترسی دیتابیس (خیلی مهم)

در بخش **Permissions** (دسترسی‌ها) چند ردیف می‌بینی.
پایینِ آن ردیف‌ها دکمهٔ **+ Add more** هست. آن را بزن.

یک ردیف خالی جدید ساخته می‌شود، با سه کادر کنار هم:

| کادر اول | کادر دوم | کادر سوم |
|---|---|---|
| **Account** | **D1** | **Edit** |

یعنی:
۱. در کادر اول بنویس/انتخاب کن: `Account`
۲. در کادر دوم بنویس: `D1`
۳. در کادر سوم انتخاب کن: `Edit`

---

## قدم ۵ — چک کردن لیست نهایی دسترسی‌ها

آخرش لیست باید این‌ها را داشته باشد (ترتیب مهم نیست):

| نوع | دسترسی | سطح | برای چه کاری |
|---|---|---|---|
| Account | **Workers Scripts** | Edit | بالا بردن خود سایت |
| Account | **D1** | Edit | ساخت دیتابیس و جدول‌ها |
| Account | **Workers KV Storage** | Edit | فایل‌های سایت |
| Account | **Account Settings** | Read | شناختن حساب شما |
| User | **Memberships** | Read | شناختن حساب شما |
| Zone | **Workers Routes** | Edit | فقط اگر دامنهٔ شخصی وصل کنید |
| Zone | **Zone** | Read | فقط اگر دامنهٔ شخصی وصل کنید |

اگر بعضی از این‌ها از قالب آماده نیامده بود، با همان **+ Add more** اضافه‌شان کن.

---

## قدم ۶ — مشخص کردن حساب و دامنه

کمی پایین‌تر:

* **Account Resources**: کادر اول را بگذار روی `Include`،
  کادر دوم را بگذار روی **اسم حساب خودت**
* **Zone Resources**: بگذار روی `Include` → `All zones from an account` → حساب خودت

---

## قدم ۷ — تاریخ انقضا (اختیاری ولی خوب)

پایین‌تر **TTL** یا **Token expiration** هست.
اگر می‌خواهی کلید بعد از مدتی خودش باطل شود، یک تاریخ بگذار.
مثلاً یک ماه بعد. اگر نمی‌خواهی، دست نزن.

---

## قدم ۸ — ساختن

۱. پایین صفحه: **Continue to summary**
۲. یک خلاصه نشانت می‌دهد → **Create Token**
۳. حالا یک رشتهٔ طولانی می‌بینی، چیزی شبیه:
   `A1b2C3d4E5f6G7h8I9j0K1l2M3n4O5p6Q7r8S9t0`

> ⚠️ **این رشته فقط همین یک بار نشان داده می‌شود.**
> کپی‌اش کن و یک جای امن نگهش دار (مثلاً یادداشت گوشی).
> اگر گمش کردی، باید یکی جدید بسازی — مشکلی نیست، کار سختی نیست.

---

## قدم ۹ — پیدا کردن «شناسهٔ حساب» (Account ID)

یک عدد دیگر هم لازم است:

۱. برو به **https://dash.cloudflare.com**
۲. از منوی سمت چپ، **Workers & Pages** را بزن
۳. در ستون سمت **راست** صفحه، پایین‌تر، نوشته **Account ID**
   با یک رشتهٔ ۳۲ حرفی زیرش
۴. روی آیکن کپی کنارش بزن

> راه دیگر: وقتی داخل پنل هستی، به نوار آدرس مرورگر نگاه کن:
> `dash.cloudflare.com/۳۲-حرف-اینجا/...` — همان ۳۲ حرف، شناسهٔ حساب است.

---

## قدم ۱۰ — حالا این دو تا را کجا بگذاریم؟

دو راه دارید. **راه اول امن‌تر است.**

### راه اول (پیشنهادی) — گذاشتن در گیت‌هاب

اینطوری کلید هیچ‌وقت داخل چت نمی‌آید و سایت هم خودکار به‌روز می‌شود.

۱. برو به صفحهٔ همین پروژه در گیت‌هاب
۲. بالا: **Settings**
۳. ستون چپ: **Secrets and variables** → **Actions**
۴. دکمهٔ سبز **New repository secret**
۵. دو تا بساز:

| Name (اسم) | Secret (مقدار) |
|---|---|
| `CLOUDFLARE_API_TOKEN` | همان رشتهٔ طولانی قدم ۸ |
| `CLOUDFLARE_ACCOUNT_ID` | همان ۳۲ حرف قدم ۹ |

۶. اگر رمز پنل مدیریت را هم خودت می‌خواهی انتخاب کنی،
   یکی دیگر بساز به اسم `ADMIN_PASS` (حداقل ۸ حرف)

تمام. از این به بعد هر تغییری که در پروژه ثبت شود،
سایت خودش روی کلادفلر بالا می‌رود.
(از تب **Actions** در گیت‌هاب می‌توانی ببینی کِی و چطور اجرا شد.)

### راه دوم — اجرا از کامپیوتر خودت

اگر می‌خواهی خودت روی لپ‌تاپ اجرا کنی:

**مک / لینوکس** — در ترمینال، داخل پوشهٔ پروژه:

```
export CLOUDFLARE_API_TOKEN="کلیدِ-قدمِ-۸"
export CLOUDFLARE_ACCOUNT_ID="۳۲-حرفِ-قدمِ-۹"
export ADMIN_PASS="یک-رمز-حداقل-۸-حرفی"
npm install
npm run deploy
```

**ویندوز** — در Command Prompt، داخل پوشهٔ پروژه:

```
set CLOUDFLARE_API_TOKEN=کلیدِ-قدمِ-۸
set CLOUDFLARE_ACCOUNT_ID=۳۲-حرفِ-قدمِ-۹
set ADMIN_PASS=یک-رمز-حداقل-۸-حرفی
npm install
npm run deploy
```

آخرش آدرس سایتت را نشانت می‌دهد.

---

## اگر کلید لو رفت چه کنم؟

نگران نباش. برو به همان صفحهٔ **API Tokens**،
روبه‌روی توکن سه‌نقطه (**…**) را بزن و **Delete** را انتخاب کن.
از آن لحظه آن کلید دیگر به درد هیچ‌کس نمی‌خورد. یکی جدید بساز.

---

## خطاهای رایج

| پیام | معنی | راه‌حل |
|---|---|---|
| `Unable to authenticate` | کلید غلط یا کپی ناقص | دوباره کپی کن، فاصلهٔ اضافه نباشد |
| `Authentication error [code: 10000]` | دسترسی کم است | قدم ۴ و ۵ را دوباره چک کن (مخصوصاً D1) |
| `More than one account available` | شناسهٔ حساب نگذاشتی | `CLOUDFLARE_ACCOUNT_ID` را ست کن |
| `not authorized to access D1` | ردیف D1 اضافه نشده | برگرد به قدم ۴ |
